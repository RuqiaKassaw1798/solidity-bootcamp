# Day 2: Token Registry
This contract allows users to register tokens with a name and symbol.

## Features:
- Uses a Struct to store token data.
- Uses a Mapping to link IDs to Tokens.
- Only the owner can deactivate a token.
- Includes an Event for registration.
